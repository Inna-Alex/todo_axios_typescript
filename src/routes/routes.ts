export const getTodos = '/todos'

export const getTodoById = (todo_id: string) => `/todos/${todo_id}`

export const postTodo = '/todos'

export const delTodo = (todo_id: string) => `/todos/${todo_id}`